name: Deploy Flask to EC2

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            git config --global --add safe.directory /home/ec2-user/golf-pos
            cd /home/ec2-user/golf-pos
            git fetch origin main
            git reset --hard origin/main
            sudo pip3 install -r app/requirements.txt
            sudo systemctl restart flaskapp
            sleep 3
            sudo systemctl status flaskapp --no-pager